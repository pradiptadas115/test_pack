version: 1.0

description: A basic workflow that runs an arbitrary linux command.

tasks:
  task1:
    action: test_pack.testpython 
    next:
      - when: <% succeeded() %>
        publish:
          - stdout: <% result().stdout %>
          - stderr: <% result().stderr %>

output:
  - stdout: <% ctx(stdout) %>